---
"gh-stack-tree": minor
---

Initial release: browser extension (PR page pill/card/rebase/join/leave, `/pulls` grouping), `gh stack-tree` CLI (`view`, `restack`, `label`, `unlabel`, `stacks`) and the agent skill.
