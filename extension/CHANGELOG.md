# gh-stack-tree

## 0.1.0

### Minor Changes

- 6431db8: Initial release: browser extension (PR page pill/card/rebase/join/leave, `/pulls` grouping), `gh stack-tree` CLI (`view`, `restack`, `label`, `unlabel`, `stacks`) and the agent skill.
